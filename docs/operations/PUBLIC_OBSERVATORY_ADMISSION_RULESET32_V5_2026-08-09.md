# Ruleset-32 public observatory admission v5 — 2026-08-09

The observer-facing surface at source commit
`cd8c83a260c0d4639c0ee0fcb7fddd0ef38b801a` is accepted for the live ruleset-32
experimental technical world. This succeeds v4 by making the pre-name identity and optional
language boundaries explicit.

Every public person and individually represented animal receives a stable numerical observer ID,
such as `P-0001` or `A-0001`, ordered by first committed appearance. IDs are finding aids and never
enter the world. If inhabitants independently develop naming, a provenance-supported emergent name
may become the primary display while the numerical ID remains its permanent audit reference.

The observer wiki now reserves an empty-by-default language archive. It makes no language claim and
does not add a language milestone. Any future dictionary must preserve original signal forms,
tentative translations, confidence, cited usage, and competing interpretations without feeding
anything back into the simulation.

The complete web lint, build, thirteen rendered/edge tests, policies, provenance, life selection,
numerical-ID determinism, and supporter isolation pass. The adjacent canonical JSON binds the
review to the exact `web/` and `docs/policies/` trees and to quality-world admission SHA-256
`1f6750a373c4d3029638091073361860ed974b3ebde7195510e986579aafd358`.
