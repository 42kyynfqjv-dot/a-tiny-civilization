# Ruleset-32 public observatory admission v4 — 2026-08-09

The observer-facing surface at source commit
`a28f99cafdd0543b3d06e77a0365dbcb97d2fdbb` is accepted for the ruleset-32 experimental world.
This succeeds v3 by adding the public life directory and dynamic individual-life records.

Every public organism can now be selected, opened, and followed. Following is browser-local
observer state and cannot reach the canonical world. Observer identifiers are stable ordinals
derived from first committed appearance, such as `Person 01`; they are explicitly not in-world
names. Individual pages distinguish organism facts from the surrounding world timeline rather
than attributing world events to an individual without evidence.

The complete web build, lint, twelve rendered edge tests, direct-HTTP redirect test, tunneled-HTTPS
test, policies, provenance, and supporter isolation pass. The adjacent canonical JSON binds the
review to the exact `web/` and `docs/policies/` trees and to quality-world admission SHA-256
`1f6750a373c4d3029638091073361860ed974b3ebde7195510e986579aafd358`.
